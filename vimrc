"==============================================================================
"  vimrc
"  Copy to $HOME/.vimrc or $HOME/_vimrc, depending on platform
"  Only function is to source vimrc_common.vim
"==============================================================================

source $HOME/.vimrc_common.vim
